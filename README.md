# ollama-chat.nvim

Neovim plugin which enables inline ollama chat within the neovim IDE without having to call any external tools.  Utilizes curl, lua, vimscript, and various other hooks/interfaces# ollama-chat.nvim
Ollama neovim inline chat plugin.  Provides a chat interface and interactive session with ollama from within neovim in a simplistic and easily configured presentation.
